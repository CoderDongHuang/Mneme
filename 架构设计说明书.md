# Mneme 架构设计说明书

## 1. 产品目标

Mneme 是具备三级记忆架构的个人学习助手。用户上传学习资料后，系统能够：

1. 基于资料回答问题，并展示文档名、页码、章节和原文片段。
2. 记住用户的表达偏好、知识薄弱点和学习进度。
3. 根据历史学习轨迹提供复习建议和下一步计划。
4. 让用户决定中等置信度记忆是否写入，避免错误画像持续污染回答。

一句话定位：Mneme 不只是回答一次问题，而是持续理解用户并帮助其学习。

## 2. 总体架构

```text
┌─────────────────────────────────────────────────────────────┐
│ React + Vite                                                │
│ 认证 / 对话 / 资料库 / 学习画像                             │
└───────────────────────┬─────────────────────────────────────┘
                        │ HTTP + SSE
┌───────────────────────▼─────────────────────────────────────┐
│ Java Gateway :8080                                          │
│ JWT认证 | 多租户校验 | 会话消息 | 资料元数据 | SSE转发       │
│ MySQL                                            Redis(可选) │
└───────────────────────┬─────────────────────────────────────┘
                        │ 内部 HTTP
┌───────────────────────▼─────────────────────────────────────┐
│ Python Agent :8001                                          │
│ FastAPI | LangGraph | RAG | 蒸馏 | 反思 | 三级记忆          │
│ Chroma :8000/本地 | DeepSeek | DashScope Embedding          │
└─────────────────────────────────────────────────────────────┘
```

### 边界原则

- 浏览器仅调用 Java，不直接信任或访问 Python。
- Java 是用户、知识库元数据、会话和消息的事实来源。
- Python 只负责 AI 推理、向量检索和语义记忆。
- `user_id` 由 Java 从 JWT 解析，禁止由前端请求头自行声明。
- 所有向量集合和长期记忆都按用户隔离。

## 3. Agent 决策链

```text
用户消息
  -> 意图识别
      -> qa: 检索指定资料库或用户全部资料库
      -> review: 读取短期与长期记忆
      -> suggest: 读取薄弱点、进度并执行反思
      -> general: 使用近期上下文直接对话
  -> 构建个性化 Prompt
  -> LLM 推理
  -> SSE/JSON 响应
  -> 记忆蒸馏与置信度分流
```

回答中的 `[1]`、`[2]` 与 `sources` 数组顺序一致。资料不足时可以用一般知识补充，但必须区分资料事实与补充建议。

## 4. 三级记忆

| 层级 | 生命周期 | 内容 | 存储 |
|---|---|---|---|
| 工作记忆 | 当前请求窗口 | 最近若干条消息 | Python 内存 |
| 短期记忆 | 会话级 | 完整历史、增量摘要 | Redis，可降级内存 |
| 长期记忆 | 跨会话 | 偏好、薄弱点、进度 | Chroma |

长期记忆采用语义去重、数量上限、薄弱点重要性累积和 30 天衰减。记忆蒸馏阈值：`>=0.8` 自动写入，`0.6-0.8` 用户确认，`<0.6` 丢弃。

## 5. 文档解析

解析流程：格式识别 -> 结构还原 -> 语义切片 -> 向量入库 -> 检索评测。

- PDF：逐页文本、表格抽取；无文本扫描件可进入 OCR。
- DOCX：标题层级、正文和表格分别处理。
- PPTX：按幻灯片保留标题、正文和表格。
- XLSX/CSV：按工作表或文件转换为结构化表格文本。
- Markdown/TXT/HTML：保留自然段与章节边界。

每个 chunk 至少携带 `document_id`、`source`、`page`、`section`、`chunk_type`。

## 6. 数据职责

MySQL 表：`user`、`knowledge_base`、`knowledge_document`、`chat_session`、`chat_message`。数据库结构由 Flyway 版本化管理。

Chroma 集合：

- `user_{userId}_kb_{kbId}`：知识片段。
- `mneme_long_term_memory`：长期记忆，通过 metadata 隔离用户和类别。

## 7. 可靠性

- LLM 客户端延迟初始化，没有 Key 时服务仍可启动并报告降级状态。
- DeepSeek 失败后当前请求可切换 Qwen；连续失败达到阈值后打开熔断。
- Redis 不可用时短期记忆退化为内存，不阻断问答。
- 文档解析异步执行，Java 保存 `parse_task_id` 并轮询真实状态。
- Python 调度器通过 FastAPI lifespan 启停，避免重复启动。
- CI 分别验证 Python、Java 和 React。

## 8. 端口约定

| 服务 | 端口 |
|---|---:|
| Chroma | 8000 |
| Python Agent | 8001 |
| Java Gateway | 8080 |
| React Vite | 5173 |
| MySQL | 3306 |
| Redis | 6379 |
