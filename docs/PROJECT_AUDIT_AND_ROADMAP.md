# Mneme 项目审计与迭代路线图

> 审计日期：2026-09-15
> 审计基线：`release/v2` 及本次修复
> 项目定位：个人与小规模团队本地自托管的 Beta 学习助手

## 1. 审计结论

Mneme 已经具备可运行的核心闭环：账号认证、资料库、异步文档解析、结构化 RAG、SSE 对话、引用展示、三级记忆、学习计划、复习卡片、测验、数据导出和 Docker 自托管。React、Java、Python 三层职责总体清楚，浏览器只访问 Java，用户身份由 JWT 注入，Python 通过内部令牌隔离。

当前版本适合作为开源 Beta 和个人自托管项目，不宜描述为完整生产级平台。主要原因不是“无法运行”，而是部分高级功能只有基础实现，真实 RAG 评测数据规模也不足。账号删除和单文档生命周期的 P0 实现已完成，但仍建议通过故障注入和多实例场景继续强化补偿机制。

## 2. 本轮验证结果

| 检查 | 结果 | 说明 |
|---|---|---|
| Python Ruff | 通过 | `ruff==0.15.17`，全量 Python 源码静态检查 |
| Python Pytest | 78 passed，1 skipped | 本机虚拟环境运行；跳过项为依赖本机 Tesseract 的 OCR 集成测试 |
| Java Maven Test | 13 passed，2 skipped | 单元/安全/工作台指标测试通过；2 个 Testcontainers/Flyway 集成测试因本机 Docker 引擎未启动而跳过 |
| 前端 ESLint | 通过 | React 源码静态检查 |
| 前端 Vitest | 2 passed | API 客户端基础契约 |
| Playwright mock E2E | 6 passed，2 skipped | 桌面与移动端；真实模型用例默认跳过 |
| Vite Build | 通过 | 生产构建成功 |
| Docker Compose config | 通过 | 基础配置与 selfhost 覆盖可合并 |
| npm 安全审计 | 通过 | Vitest 已升级到 `4.1.11`；0 moderate、0 high、0 critical |

自动化测试通过只能证明已覆盖路径没有回归，不能证明全部业务语义完整。下面按功能给出实际成熟度。

本轮离线 RAG 基线为 110 条虚构结构查询，其中 105 条可回答、5 条无答案：`Recall@5=1.0`、`MRR=0.8943`、`faithfulness_proxy=0.8762`、`citation_recall=1.0`、`abstention_proxy=1.0`、P95 检索延迟约 `15.13ms`。`faithfulness_proxy` 是关键事实覆盖 proxy，不是 RAGAS 或真实 LLM Faithfulness。该结果仍不代表复杂扫描 PDF、真实模型或大规模资料质量。

## 3. 功能成熟度

### 3.1 认证与用户资料：基本到位

已实现注册、登录、JWT、HttpOnly Cookie、退出、资料修改、密码修改、密码重置、头像和账号删除。Java 会从可信 JWT 中取得用户 ID，不接受前端伪造身份。密码使用 BCrypt，重置令牌只保存 SHA-256 摘要并设置 15 分钟有效期。

本轮修复了头像存储路径：Docker 环境统一写入 `/data/avatars`，由现有 `./data:/data` 挂载持久化，避免重建 Java 容器后头像消失。

本轮已补齐：删除账号会先调用 Python 清理 Chroma 集合、长期记忆、Redis 短期会话和会话元数据，再清理本地资料与头像，最后让 MySQL 外键级联删除业务数据；任何外部清理失败都会阻止用户行删除。后续仍应将该同步流程升级为带状态和重试的账号删除任务，覆盖进程中断和多实例场景。

### 3.2 资料库与文档解析：核心可用

支持 PDF、DOCX、PPTX、XLSX/XLSM、CSV、Markdown、TXT、HTML。PDF 包含文本块排序、重复页眉页脚过滤、表格抽取、低文本页 OCR 和可选多模态图片理解。解析失败会进入重试任务，并把错误状态返回前端。

本轮修复 Java/Python 格式契约不一致：`.markdown` 现在可以通过网关上传。

本轮已补齐单文档删除和重新解析：两者都通过 Java 任务队列执行，删除会清理 Chroma 片段、原文件和 MySQL 元数据；重新解析复用原 `document_id`，先删除旧片段再写入新片段，避免重复召回。仍需改进：

- 增加文档替换版本、版本历史和页级解析报告。
- 上传校验应继续加入 MIME/魔数映射、压缩包解压大小限制和恶意文档扫描。
- OCR 应记录页级置信度；低置信度页面需要在前端提示用户核验。
- 表格、公式、多栏和流程图需要建立专项测试集，不能只验证“能抽出文本”。

### 3.3 RAG 与引用：可用但评测不足

检索结合 Dense、BM25 风格词法评分和 RRF 融合，包含确定性查询改写、重复片段过滤和上下文预算。chunk 带文档 ID、来源、页码、章节和类型，回答引用可以回到证据片段。资料不足时 Prompt 要求区分资料事实与一般知识。

仍需改进：

- 已增加 SQLite FTS5 持久化词法索引，文档写入、替换、删除和用户/知识库清理会同步维护；索引不可用时保留 Chroma 扫描降级。
- 已增加可选、懒加载的 Cross Encoder 重排接口；默认关闭，未安装 `sentence-transformers` 或模型不可用时确定性降级到 RRF。
- 评测集已扩展到 110 条，覆盖 PDF、CSV/XLSX、PPTX、Markdown、HTML、解析策略、无答案问题和多主题查询；真实扫描 PDF、复杂表格、多栏和跨页仍需人工标注。
- 已增加 Recall@K、MRR、Faithfulness proxy、Citation Precision/Recall、P95 延迟和输入费用估算；真实 LLM 忠实度、Answer Relevance 和模型费用仍需单独运行。

### 3.4 Agent 与 Prompt：基础路由到位

LangGraph 已区分问答、回顾、建议和普通交流，并按意图选择知识与记忆上下文。Prompt 构建、模型调用和记忆回写已经拆分，不是单一接口内堆叠全部逻辑。

仍需改进：意图识别目前依赖一次 LLM 分类，异常时退回 general。建议增加规则快速路径、结构化输出重试、节点级超时和执行轨迹持久化。复杂任务应引入显式工具调用，而不是仅靠 Prompt 描述行为。

### 3.5 三级记忆：核心机制可用

工作记忆、Redis 短期记忆和 Chroma 长期记忆已经接通。长期记忆支持偏好、薄弱点、进度、语义去重、重要度、冻结和用户确认。Redis 不可用时降级到进程内存。

仍需改进：

- 记忆确认在 Java 与 Python 间有两套状态，需要增加最终一致性和幂等测试。
- 将同步账号清理升级为带状态、重试和故障恢复能力的删除任务。
- 增加记忆来源、证据消息、版本和撤销历史，使画像变化可解释。
- 冻结、过期和反思策略需要更多跨会话 E2E，而不只是单元测试。

### 3.6 学习工作台：功能存在但部分为基础版

学习计划、复习卡、SM-2 风格间隔调整、知识测验、会话分支、检索调试和导出均有真实数据库实现。

需要明确边界：

- 测验已支持基于证据的结构化 LLM 出题、确定性回退和关键点评分，但尚未有大规模题目质量评测。
- 导出与导入已支持关系型学习数据；原文件和向量索引仍需重新上传构建。
- 会话分支复制消息，但需要补充来源消息归属校验和并发分支测试。
- 工作台已提供按用户隔离的计划完成率、复习间隔、到期卡片、测验平均分、错题转卡 proxy 和待确认薄弱点指标；指标历史趋势和跨设备分析仍未实现。

### 3.7 可靠性与可观测性：基础到位

已有任务重试、废弃锁恢复、模型主备切换、熔断、Redis 降级、健康检查、Prometheus 指标、trace ID 和审计日志。Caddy 已将 `/api/*`、`/ws/*` 与 SPA fallback 分离。

仍需改进：任务领取在单机 MySQL 下可用，多实例部署时应使用数据库原子条件更新并增加并发测试；关键跨存储操作需要 outbox/saga；日志应避免记录资料正文和个人敏感信息。

## 4. 本轮修复清单

1. 将间接依赖 `nanoid` 固定到无当前高危公告的版本，恢复 `npm run audit` 与 CI。
2. Java 上传白名单增加 `.markdown`，与 Python 解析器和 README 保持一致。
3. 头像路径改为配置项 `AVATAR_STORAGE_PATH`；Docker 默认 `/data/avatars`，本地默认 `../data/avatars`。
4. README 修正真实 E2E 描述，并加入本审计文档入口。
5. 移除已经失效的 React Router 安全 allowlist，当前没有 high/critical 审计例外。
6. 用 SQLite FTS5 替换查询时全量词法扫描，补充可选重排器、105 条离线评测和工作台学习指标。

## 5. 下一阶段实施顺序

### P0：数据一致性与可信删除

目标：用户点击删除后，MySQL、Chroma、Redis、资料文件和头像都可验证地清理。

1. 新增 `account_deletion_task` 或通用 outbox 事件。
2. 账号先进入 `deleting` 状态并禁止新请求。
3. 分步骤删除知识集合、长期记忆、短期会话、资料文件和头像。
4. 每一步记录状态、错误和重试次数；全部完成后再删除/匿名化用户行。
5. 增加故障注入测试：Python 不可用、文件被占用、任务重复执行、服务中途重启。

### P0：单文档生命周期

增加单文档删除、重新解析、替换版本和解析报告。删除必须同时清理 Chroma 中该 `document_id` 的 chunk，重新解析需要幂等键，避免重复向量。

### P1：RAG 质量升级（已实施基础能力）

1. 建立 BM25 + Dense Hybrid Retrieval。
2. 增加查询改写和 Cross Encoder 重排。
3. 根据 token 预算动态选择片段并去除重复上下文。
4. 建立至少 100 条虚构但接近真实结构的评测集。
5. CI 跑离线召回指标，定期任务跑真实模型忠实度与费用评估。

已实现 FTS5 BM25 风格词法索引与 Dense 的 RRF 混合召回、确定性查询改写、重复片段过滤和动态上下文预算，并提供可选 Cross Encoder 接口。评测集已达到 110 条，但仍是虚构离线数据；人工标注、真实 Cross Encoder 模型和 LLM 忠实度评测仍属于后续质量工程。

### P1：测验与学习闭环（已实施）

让 LLM 按检索证据生成结构化题目，服务端用 JSON Schema 校验。选择题保存解析与证据；简答题采用“关键点覆盖 + 引用证据 + LLM 评分”并展示评分依据。错题自动生成复习卡并写入薄弱点候选记忆。

题目现在由 LLM 严格依据检索证据生成，并经 Pydantic/服务端双重结构校验，保存关键点、证据和来源；模型不可用或输出不合法时自动使用确定性证据题目。简答题按关键点覆盖评分，反馈显示依据；错题自动生成复习卡和待确认薄弱点记忆。

### P1：完整导入导出（已实施关系数据）

为导出定义版本化 JSON Schema，使用旧 ID 到新 ID 的映射恢复会话、消息、计划、复习卡、测验和分支。资料原文件和向量应通过独立压缩包导入，并限制压缩炸弹与路径穿越。

当前 schema `mneme.workspace` v2 已完整恢复关系型学习数据及测验作答记录，并设置单类数据 10000 条、文本长度和 JSON 合法性限制。归档包额外携带受限的原始文件副本和文档清单，但不会导入服务器文件，也不会恢复 Chroma 向量；用户仍需重新上传资料以重建索引。归档 manifest 不包含服务器内部 `file_path`，并拒绝非法 ZIP、路径穿越、未知条目、文件数量超限和解压大小超限。

### P2：体验与运维（基础项已实施）

- 前端已增加逐文件上传队列、成功/失败状态和错误详情；文档级重试与任务中心已存在。
- 引用原文高亮、页码定位、失败任务重试和任务通知已具备基础实现；页级解析报告仍待补充。
- 模型、Embedding 与多模态配置检查页已提供脱敏状态，不在浏览器保存密钥。
- 增加 OpenTelemetry、结构化性能指标和慢检索追踪。
- 提供数据迁移命令、备份校验和恢复演练脚本。

Python `/health/config` 已提供不含密钥值的配置状态，Java 已代理该接口，前端“服务配置”页可查看就绪状态；密钥本身不会进入浏览器。

## 6. 本轮 P2 验证与问题清单

P2 修改后已执行 Python 测试与离线 RAG 基线、Java、前端和 Compose 检查。已知不属于产品故障的环境提示包括：部分 Chroma/遥测客户端组合可能输出兼容性日志，以及无 Tesseract 的环境会跳过 OCR 集成测试。Java 动态 agent 提示已通过 Surefire 参数消除；本机 Ruff 已通过，CI 仍配置为强制执行 Ruff。

仍需提出并跟踪的问题：

1. 上传队列已持久化到浏览器 localStorage，服务端任务通知中心已提供基于 `processing_task` 的历史查询和 JVM 内 SSE；尚未实现独立通知表、跨实例广播和历史清理策略。
2. `/health/config` 已有 Java 代理和前端诊断页；密钥轮换和更细的管理员权限隔离仍待实现。
3. OCR 解析结果已写入页级置信度元数据；复杂表格/多栏/公式视觉解析和跨文档冲突评测仍不完整。
4. RAG 已接入持久化 FTS5、可选 Cross Encoder 接口和 110 条离线指标评测；人工标注集、真实模型 Faithfulness/Answer Relevance 仍待扩充。
5. 账号删除已做幂等清理和故障注入测试；数据库状态化删除、多实例恢复演练仍待实现。
6. 归档导出已接入工作台，导入会恢复关系数据并明确提示原始文件和向量索引需要重新建立；Java 已覆盖非法 ZIP、路径穿越、文件数量、单文件和总解压大小边界。

## 7. 发布标准

下一次版本发布至少满足：

- 所有 CI 作业通过，安全审计无未解释的 high/critical。
- Docker 从空数据目录启动后可完成真实注册、上传、解析、RAG 与引用。
- 账号和资料删除通过跨存储一致性 E2E。
- RAG 基线有明确数据规模、阈值和失败样例，不用单个 1.0 指标宣传整体质量。
- README、接口文档、配置模板和实际端点保持同步。
- 变更日志明确兼容性、迁移步骤和已知限制。

## 8. 当前推荐版本定位

推荐 GitHub 简介继续使用：

> Mneme 是一个支持结构化 RAG、三级记忆和可追溯引用的开源个人学习助手，采用 React、Spring Boot、FastAPI、LangGraph、Chroma、MySQL 与 Redis，面向本地自托管。

发布标签应使用 `Beta`，不要使用“生产级”“支持任意复杂文档”或“完全准确”等无法由当前测试证明的表述。

## 9. 后续功能与优化方向

### 近期

- [~] 实现持久化通知中心和 SSE 任务进度（当前复用任务表，SSE 订阅仅单 JVM，历史清理待补）。
- [x] 增加引用点击后的原文定位、高亮和页码预览基础能力。
- [~] 增加 Java 配置诊断、密钥轮换提示和脱敏审计基础能力（诊断与提示完成，实际轮换和管理员隔离待补）。
- [x] 为导出增加可选资料归档包，并加入压缩炸弹、路径穿越和容量限制校验及 Maven 自动化边界测试。

### 中期

- [~] 引入 SQLite FTS5 专用词法索引和可选 Cross Encoder 重排接口（索引完成，实际模型默认关闭）。
- [x] 建立 110 条覆盖 PDF、表格、解析策略、否定/无答案问题的离线评测集；真实扫描 PDF 与人工标注仍需扩充。
- [x] 增加等价离线评测：Recall@K、MRR、Faithfulness proxy、Citation Precision/Recall、P95 延迟和费用估算。
- [x] 增加学习目标完成率、复习间隔效果、错题转卡 proxy 和待确认薄弱点指标。

### 长期

- 以 outbox/Saga 重构账号删除、文档生命周期和备份恢复。
- 多租户隔离、对象存储、向量库分片和水平扩展。
- 多模态文档问答、图表/公式理解和引用级视觉证据。
- 插件化工具调用、可审计 Agent 轨迹和用户可控的记忆版本回滚。
